# Runtime dependencies. The core code sticks mostly to Python stdlib, while these
# packages are available for stronger source adapters as the board list expands.
httpx>=0.27
beautifulsoup4>=4.12
python-dateutil>=2.9
