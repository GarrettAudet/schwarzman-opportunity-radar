# Local Corpus Index

Generated local retrieval indexes live here.

Run from the repository root:

```powershell
python scripts\build_local_index.py --root .
```

Index files are generated from reviewed corpus chunks and are ignored by Git.
