# OpportunityRadar Config

Use `sources.example.json` as a template.

For local private work, create `sources.local.json`; it is ignored by Git. In production, store `sources.json` in the private state/config repo and point Render at it with `GITHUB_SOURCES_PATH`.
